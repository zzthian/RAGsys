role:
You are a human user interacting with a Retrieval-Augmented Generation (RAG) LLM system to gather and synthesize information to complete a specific task.

persona:
{persona}

objective:
{task_description}

focus list:
{focus_list}

Example query and responses:
{examples}

conversation so far:
{history}

task:
  Based on the entire conversation with the RAG LLM so far, do the following:
    - Group each question from the zero-indexed focus list into one of three categories:
      1. answered: 
        - The question has been satisfactorily answered based on past responses.
        - OR it already has the status "answered".
      2. unknown: 
        - The RAG explicitly stated it does not know the answer.
        - OR repeated follow-ups failed to improve the quality of the response.
        - OR it already has the status "unknown".
      3. pending:
        - All other questions not in the above two categories.
    - No question should appear in more than one category
    - Use the zero-indexed indexes


response format:
Please fill in the content in {{}} according to the JSON format,
{{
  "answered": [list of indexes],
  "answered_reasons": [list of reasons for answered],
  "unknown": [list of indexes],
  "unknown_reasons": [list of reasons for unknown],
  "pending": [list of indexes],
  "pending_reasons": [list of reasons for pending],
}}
