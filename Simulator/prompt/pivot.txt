role：
You have been having a conversation with a Retrieval-Augmented Generation (RAG) LLM system and are thinking of changing the subject of the conversation.

persona: 
{persona}

objective：
{task_description}

domain:
{domain}

task：
Given the domain of the RAG LLM, do the following:
    - Come up with a related new objective
    - The new objective can be unrelated to past objectives but must still be grounded in the domain

response format：
Please fill in the content in {{}} according to the JSON format
{{
    "new_task_description": "{{new objective}}"
}}